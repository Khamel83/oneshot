# Global Claude Code Config — Khamel

Do the simple thing first. Don't write scripts when a CLI exists.
Don't debug for an hour when restarting might fix it.

Before using any external API or config syntax, check current docs — training data goes stale.

See `~/.claude/rules/` for infrastructure, stack defaults, and lessons.
