# Task: [Title]

**Created**: [date]
**Risk Level**: low | medium | high
**Task Class**: [plan | research | implement_small | implement_medium | test_write | review_diff | doc_draft]

## Goal

[What this task accomplishes. One paragraph. Be specific about the outcome.]

## Non-Goals

- [What this task explicitly does NOT do]
- [Boundary conditions or out-of-scope items]

## Constraints

- [Tech constraints — e.g., "must use existing Python router", "no new dependencies"]
- [Time constraints — e.g., "must finish in one session"]
- [Things to avoid — e.g., "do not modify existing API contracts"]

## Acceptance Criteria

- [ ] [Criterion 1 — verifiable, specific, e.g., "auth handler returns 401 for missing token"]
- [ ] [Criterion 2]
- [ ] [Criterion 3]

## Files Involved

### Will Change

- `path/to/file` — [what changes and why]

### Read-Only (for context)

- `path/to/file` — [why needed, e.g., "existing schema to follow"]

### Must NOT Touch

- `path/to/file` — [why protected, e.g., "production config, changes tracked separately"]

## Verification Commands

```bash
# Tests
[command to run tests, e.g., "pytest tests/ -v"]

# Lint
[command to run lint, e.g., "shellcheck scripts/"]

# Type check
[command to run typecheck, e.g., "pyright api/"]
```

## Planned Steps

1. [Step 1 — e.g., "Add new handler function in api/handlers/foo.py"]
2. [Step 2 — e.g., "Wire handler into router in api/index.py"]
3. [Step 3 — e.g., "Add tests and verify"]

## Rollback

[How to undo if things go wrong — e.g., "git checkout -- <files>"]
[Or: "git revert <commit>" if already committed]

## Human Decisions Required

- [Decision 1]: [options, or "none" — e.g., "Which error code for rate limit: 429 or 503?"]
