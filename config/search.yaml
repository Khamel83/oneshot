search:
  default_backend: argus
  base_url: http://100.112.130.100:8270
  api_key_env: ARGUS_API_KEY
  modes:
    discovery:
      providers: [searxng, brave, exa]
      max_results: 10
    grounding:
      providers: [serper, tavily]
      max_results: 5
    recovery:
      providers: [searxng, duckduckgo, yahoo]
      max_results: 10
    research:
      providers: [searxng, brave, exa, tavily]
      max_results: 15
    aliases:
      cheap:
        mode: discovery
        providers: [searxng]
        max_results: 5
      precision:
        mode: grounding
        providers: [serper, tavily]
        max_results: 5
