workers:
  local:
    host: localhost
    transport: local
    role: planner
    harness: claude_code

  oci:
    host: oci-dev
    transport: ssh
    role: planner
    harness: claude_code

  macmini:
    host: macmini
    transport: ssh
    role: worker
    harness: opencode

  homelab:
    host: homelab
    transport: ssh
    role: worker
    harness: opencode

  claw:
    host: localhost
    transport: local
    role: worker
    harness: claw_code
    install_path: ~/github/claw-code-agent
